---
# defaults file for elastic_kibana

# kibana values:

directory_path_for_deb_pack_elastic: "{{ directory_path_for_deb_pack }}/{{ elastic_deb }}"

directory_path_for_deb_pack_kibana: "{{ directory_path_for_deb_pack }}/{{ kibana_deb }}"